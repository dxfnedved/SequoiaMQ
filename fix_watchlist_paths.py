import re; import sys; def fix_watchlist_api_paths(file_path): with open(file_path, 'r', encoding='utf-8') as file: content = file.read(); content = re.sub(r'return del\(/watchlist/', r'return del(/api/watchlist/', content); content = re.sub(r'return put\(/watchlist/', r'return put(/api/watchlist/', content); with open(file_path, 'w', encoding='utf-8') as file: file.write(content); print(f'已修复文件: {file_path}'); if __name__ == '__main__': fix_watchlist_api_paths('frontend-vue/src/api/watchlist.js')
